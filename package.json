{
  "name": "chempredict",
  "version": "1.0.0",
  "description": "Interactive Periodic Table and Reaction Predictor",
  "main": "api/elements.js",
  "scripts": {
    "dev": "npx vercel dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "mongodb": "^6.3.0"
  }
}
